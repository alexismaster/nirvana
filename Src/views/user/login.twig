{# ФОРМА АВТОРИЗАЦИИ #}

{% extends "index.twig" %}


{% block title %}Авторизация{% endblock %}

{% block meta %}
    {% if session.user %}<meta http-equiv="refresh" content="5; url=/" />{% endif %}
{% endblock %}

{% block content %}
    <h1>Авторизация</h1>
    <p><br></p>

    {# Пользователь уже авторизирован #}
    {% if session.user %}
        <p>Вы уже авторизированы. Через 5 сек вы будете перенаправены на главную страницу.</p>

    {% else %}
        <form method="POST">
            <div class="form-group">
                <label for="email">Email address</label>
                <input type="email" class="form-control" name="email" value="{{ values.email }}" placeholder="Email">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" class="form-control" name="password" value="{{ values.password }}" placeholder="Password">
            </div>

            <div class="form_errors">
                <p>{{ errors.email }}</p>
            </div>

            <button type="submit" class="btn btn-default">Submit</button>
        </form>
    {% endif %}

{% endblock %}